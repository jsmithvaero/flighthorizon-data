# Playing around a bit with R to see if we can do anything cool with it.
install.packages("devtools")
devtools::install_github("tylermorganwall/rayshader")

myString <- "Hello, World!"
print (myString)